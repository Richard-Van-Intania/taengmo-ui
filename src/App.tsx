import { MyButton } from "./components/MyButton";

export default function App() {
  return (
    <div>
      <h1 className="text-3xl font-bold underline bg-primary-dev">Hello world!</h1>
      <MyButton onClick={() => {}}></MyButton>
    </div>
  );
}
