type Props = {
    label: string;
    checked: boolean;
    updateChecked: (value: boolean) => void;
};
export default function CustomCheckbox({ label, checked, updateChecked }: Props): import("react/jsx-runtime").JSX.Element;
export {};
