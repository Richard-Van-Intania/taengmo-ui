type Props = {
    label: string;
    iconify: string;
    enable: boolean;
    whenClicked: React.MouseEventHandler<HTMLButtonElement>;
};
export default function ButtonMenuIcon({ label, iconify, enable, whenClicked }: Props): import("react/jsx-runtime").JSX.Element;
export {};
